
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /images/{userId}/{fileName} {
      allow read: if true; // Allow public reads for image URLs
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
