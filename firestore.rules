
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all collections.
    // This is for initial setup and seeding.
    // WARNING: You should secure these rules before deploying to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
